#include<bits/stdc++.h>
using namespace std;

void code(){} 
void eat(){}
void sleep(){} 
int main(){
    vector<int> life; 
    bool dead = false; 


    for(int i = 0; i < life.size(); i++){
        if(!dead) {
            code(); 
            eat(); 
            sleep();
        } else {
            break; 
        }
    }
    return 0;
}